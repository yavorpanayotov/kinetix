apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ai-insights-service.fullname" . }}-config
  labels:
    {{- include "ai-insights-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
