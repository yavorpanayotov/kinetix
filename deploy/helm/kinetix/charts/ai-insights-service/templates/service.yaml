apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-insights-service.fullname" . }}
  labels:
    {{- include "ai-insights-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-insights-service.selectorLabels" . | nindent 4 }}
