apiVersion: v1
kind: Pod
metadata:
  name: {{ include "demo-orchestrator.fullname" . }}-test-health
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: test-health
      image: busybox
      command: ["wget", "-qO-", "http://{{ include "demo-orchestrator.fullname" . }}:{{ .Values.service.port }}/health"]
