apiVersion: v1
kind: Service
metadata:
  name: {{ include "fix-gateway.fullname" . }}
  labels:
    {{- include "fix-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "fix-gateway.selectorLabels" . | nindent 4 }}
